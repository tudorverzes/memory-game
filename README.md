Manual tests must be run manually from terminal after running the junit tests and saving the test output in .exec format.
The execution of manual tests follows the linearly independent paths found with the help of control flow graphs.
